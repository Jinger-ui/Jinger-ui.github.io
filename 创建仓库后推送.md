# 创建 Jinger-ui.github.io 后一键推送

先在 GitHub **网页上新建空仓库**（当前环境未检测到 `gh`，无法自动建库）：

1. 打开：<https://github.com/new>
2. **Repository name** 填：**`Jinger-ui.github.io`**（须与你的 GitHub 用户名一致）
3. 选 **Public**
4. **不要**勾选 README、.gitignore、License（保持空仓库）
5. 点击 **Create repository**

建好后在本机执行：

```bat
cd /d "c:\Users\csls\Desktop\Huang Jingjia_Resume_Update\prism-portfolio"
git push -u origin main
```

若提示认证，请用 **HTTPS + PAT** 或改用 **SSH** 并执行：

```bat
git remote set-url origin git@github.com:Jinger-ui/Jinger-ui.github.io.git
git push -u origin main
```

推送完成后：**Settings → Pages → Source** 选 **GitHub Actions**，详见 `部署到GitHub-Pages.md`。
